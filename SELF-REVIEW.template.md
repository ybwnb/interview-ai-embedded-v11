# SELF-REVIEW

**候选人：** ___________
**提交时间：** ___________
**仓库 URL：** ___________

## 完成情况

勾选已完成的模块，未完成的简述卡在哪里：

- [ ] A INA3221 移植（读 Arduino 参考 → 移植为 ESP-IDF 组件 + 自写 CMakeLists.txt + UART 打印电压电流验证）
- [ ] B config_manager 核心（init / load / save / from_json / to_json 全部实现）
- [ ] C main.c 解耦（删硬编码 + 接入 config_manager_load + sensor_task 变色）
- [ ] D 运行时配置更新（UART JSON 命令 + NVS 持久化 + 重启验证）

未完成说明：

> （写不下去的原因，不要跳过）

---

## 四个问题（必答，不限字数，写真实经历）

### 1. 追问过程

> 贴出你认为最有价值的一轮 AI 对话——你最初问了什么，AI 哪里回答得不够或有误，你怎么追问的，最终得到了什么结论？

（粘贴原始 prompt + 你的判断，不要转述）

---

### 2. 卡住的地方

> 你卡得最久的地方是什么？花了多长时间？期间走过哪些死路，最后怎么解开的？

（具体到报错信息或 UART 日志现象，不要只写"debug 了很久"）

---

### 3. 架构延伸

> config_manager 目前把整个 JSON 作为一个字符串存入 NVS。  
> 假设配置字段增加到 50 个，部分字段需要单独原子更新（不能覆盖其他字段），你会怎么改设计？  
> 另外，如果同一份配置需要同时支持 NVS 持久化和 OTA 下发（通过 MQTT），你的 config_manager 接口需要怎么扩展？

（写你自己的判断，不需要实现）

---

### 4. 回头看

> 做完之后，你觉得这个设计里哪个决策值得质疑？如果让你重来，你会改什么？

（可以质疑题目本身的设计）

---

## Claude Code 使用证据

贴出你认为最关键的 3 条 prompt 原文（不是描述，是原文）：

**Prompt 1：**
```
（粘贴）
```

**Prompt 2：**
```
（粘贴）
```

**Prompt 3：**
```
（粘贴）
```

---

## 已知 Bug / 未处理边界

- 
- 
